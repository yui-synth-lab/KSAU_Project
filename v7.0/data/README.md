# Data Compression Notice

The large\text file `CJTwist.-2.txt` has been compressed to `CJTwist.-2.txt.gz` to optimize repository size.

To use this file with legacy scripts, please decompress it using:
- `gzip -d CJTwist.-2.txt.gz` (CLI)
- Or use the `gzip` module in Python to read it directly.
