# Review — Iteration 3: MODIFY

**査読日:** 2026-02-23
**判定:** MODIFY

## 1. 概要
仮説 H15「Algebraic Mapping to TQFT CS Level (Discrete)」の初期設計において、離散的な量子化アルゴリズムの提案が行われましたが、科学的検証のプロセスおよび SSoT 準拠の観点から複数の重大な不備が認められます。特に、必須とされている統計的検定（FPR, p値）の欠如および、SSoT 定数の無視は AIRDP フレームワークの基本原則に反します。

## 2. 独立検証結果
- **再現性:** 完了。`discrete_cs_design.py` を実行し、報告された数値（Witten 整合性 100%, 相関 r=0.4823）を再現しました。
- **データ真正性:** KnotInfo の実データを使用していることを確認しました。
- **統計的妥当性:** **極めて不十分**。
    - $N=12$ における $r=0.4823$ の p 値は約 0.11 であり、成功基準の 0.025 を大幅に上回っています（有意ではない）。
    - 実行手順 Step 5 で「必須」とされている FPR テストが実施されていません。
    - 100% の整合性は「約数から選択する」というアルゴリズムの設計上当然の結果（トートロジー）であり、その選択に物理的有意性があるかが検証されていません。

## 3. SSoT コンプライアンスチェック
- **SSoT 違反:** ロードマップおよび H15 定義で参照が義務付けられている `constants.json` の `k_mapping_coefficients` が全く使用されていません。独自のアルゴリズムを採用する場合は、既存の SSoT との整合性または置き換えの正当性を証明する必要があります。
- **マジックナンバー:** Fallback 値としての `best_k = 1` など、SSoT に根拠のないハードコードが散見されます。

## 4. 統計指標
- p 値（観測）: ~0.112 (算出値)
- Bonferroni 補正後閾値: 0.025
- FPR: 未計算 (要修正)
- Witten 整合性レート: 100.00% (アルゴリズムによる構築)

## 5. 修正要求事項
次のイテレーションとして認めるためには、以下の修正が必要です。

1. **FPR テストの実施:** 順列検定（Permutation Test）等を用い、提案アルゴリズムの「約数選択」がランダムな位相入力に対しても同様の整合性や相関を示すかどうかを検証し、FPR を算出してください。
2. **SSoT への準拠または拡張:** `k_mapping_coefficients` を使用するか、現在のアルゴリズムを SSoT に正式に組み込む提案（Orchestrator 経由）を行ってください。
3. **データセットの拡大:** $N=12$ では統計的有意性の確保が困難です。KnotInfo の全データセット（あるいは一定の交点数範囲）に対してアルゴリズムを適用し、広範な統計的有意性を実証してください。
4. **報告の不備:** `results.json` に FPR および p 値の項目を追加し、正直に記録してください。
