# Review — Iteration 5: MODIFY

**査読日:** 2026-02-26
**判定:** MODIFY

## 概要
$s=0$ 粒子での発散を解消し、エントロピー項 $\ln(Det)$ を統合した精緻化 TSI 公式の構築は、崩壊幅の幾何学的記述において前進です。しかし、物理定数のハードコード（SSoT 違反）、マジックナンバーの混在、および必須とされている統計的検証（FPR）の欠如が確認されました。また、算出された $R^2$ はロードマップの撤退基準を下回っています。

## 査読詳細

### 1. 科学的誠実性（不十分）
- **虚偽報告:** `results.json` で `hardcoded_values_found: false` と報告されていますが、実際には公式内の `+ 1.0` および `tsi_refinement.py` 内の寿命データ（21-29行目）がハードコードされています。
- **透明性:** $R^2$ が低い（0.6399）ことは正直に報告されていますが、撤退基準（0.7）への抵触についての言及がありません。

### 2. SSoT コンプライアンス（不合格）
- **物理データのハードコード:** `tsi_refinement.py` における Muon, Tau, Top 等の実験値記述はプロジェクト規約への重大な違反です。
- **SSOT ローダー:** `tsi_final_evaluation.py` では `SSOT()` クラスを正しく利用していますが、探索スクリプト側での規約遵守が欠落しています。

### 3. 統計的妥当性（不十分）
- **FPR テスト:** 実施されていません。ランダムなトポロジー割り当てにおいて、偶然 $R^2 = 0.64$ という相関が得られる確率（FPR）を定量化してください。
- **撤退基準:** $R^2 = 0.6399$ は撤退基準（$R^2 < 0.7$）を下回っています。次回のイテレーションで 0.7 を超える成果が得られない場合、H43 は REJECT（STOP）判定となります。

### 4. 物理的根拠
- **正則化の必然性:** $s+1.0$ の $1.0$ を単なるフィッティングパラメータではなく、幾何学的最小不変量（例：自明でない位相構造の最小単位）として再定義してください。

## 統計指標（再現結果）
- 決定係数 R²: 0.6399 (不合格 - 閾値 0.7)
- p 値: 0.009637 (合格 - Bonferroni 閾値 0.0166)
- FPR: 未算出 (不合格)
- MAE (log %): 算出結果は報告通り再現。

## 判定根拠
SSoT 違反（物理定数のハードコード）、FPR 未算出、および撤退基準（R² < 0.7）への抵触により、MODIFY と判定します。
